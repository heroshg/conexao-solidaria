# Dev/demo credentials only (matches the connection strings baked into appsettings.json for
# local runs). In a real deployment this would come from a proper secret store, not git.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: conexao-solidaria
type: Opaque
stringData:
  POSTGRES_USER: postgres
  POSTGRES_PASSWORD: postgres
